on:
  push:
  pull_request:
    types: [opened, synchronize]
jobs:
  build:
    runs-on: ubuntu-latest
    name: script/cibuild-node
    steps:
      - uses: actions/checkout@v3
      - name: test
        run: script/cibuild-node
